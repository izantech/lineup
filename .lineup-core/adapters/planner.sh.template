#!/usr/bin/env bash
set -euo pipefail
# Lineup reference adapter: Planner (Architect)
# Reads native plan request payload on stdin, outputs lineup/v3 Plan YAML on stdout

SYSTEM_PROMPT=$(cat "{{SYSTEM_PROMPT_PATH}}")
PAYLOAD="$(cat)"

# {{HOST_INVOKE_COMMAND}} is replaced with the host-specific CLI invocation.
# Example for Claude:
#   claude --print --bare --model {{MODEL}} -s "$SYSTEM_PROMPT" "$PAYLOAD"
{{HOST_INVOKE_COMMAND}}
