#!/usr/bin/env bash
set -euo pipefail
# Lineup TF Adapter: Validator (Reviewer)
# Reads ValidationRequest JSON on stdin, outputs {status, reason} JSON on stdout

SYSTEM_PROMPT=$(cat "{{SYSTEM_PROMPT_PATH}}")
PAYLOAD="$(cat)"

# {{HOST_INVOKE_COMMAND}} is replaced with the host-specific CLI invocation.
# Example for Claude:
#   claude --print --bare --model {{MODEL}} -s "$SYSTEM_PROMPT" "$PAYLOAD"
{{HOST_INVOKE_COMMAND}}
