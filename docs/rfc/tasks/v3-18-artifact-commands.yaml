apiVersion: lineup/v3
kind: RfcTask
id: V3-18
title: Add artifact commands (validate, artifacts show/path/diff)
status: done
last_updated: 2026-04-12
depends_on:
  - V3-16
parallel_wave: 7
suggested_agent: developer
rfc_sections:
  - Artifact and Contract Rules
  - Validation and persistence
read_scope:
  - cli/src/lib/artifact-store.ts
  - cli/src/lib/validation.ts
  - cli/schemas/
write_scope:
  - cli/src/cli.ts
  - cli/src/commands/
deliverables:
  - Add lineup validate command running AJV schema validation on any artifact file
  - Add lineup artifacts show <kind> displaying the latest or specified artifact contents
  - Add lineup artifacts path <kind> printing the filesystem path for scripting
  - Add lineup artifacts diff <kind> showing a diff between two runs or between latest and a run
  - Support --run <id> to scope artifact operations to a specific run
acceptance_criteria:
  - lineup validate reports schema errors with stable, structured output
  - lineup artifacts show renders YAML/JSON artifacts with proper formatting
  - lineup artifacts diff produces a readable unified diff
  - All commands support --json output
  - Invalid artifact kinds produce clear error messages listing valid kinds
notes:
  determinism_rules:
    - Artifact reads go through the existing content-addressed store
    - Diff output is deterministic given the same two inputs
