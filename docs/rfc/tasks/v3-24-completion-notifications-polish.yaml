apiVersion: lineup/v3
kind: RfcTask
id: V3-24
title: Add shell completion, notifications, DAG viz, and polish
status: done
last_updated: 2026-04-12
depends_on:
  - V3-20
  - V3-21
parallel_wave: 9
suggested_agent: developer
rfc_sections:
  - Architecture
read_scope:
  - cli/src/cli.ts
  - cli/src/lib/workflow.ts
write_scope:
  - cli/src/cli.ts
  - cli/src/commands/
deliverables:
  - Add shell completion for bash, zsh, and fish via commander's built-in support
  - Add optional notification hooks for long-running or failed runs
  - Add lineup dag command visualizing workflow and compiled task graphs in ASCII
  - Add opt-in auto-commit policy configurable via .lineup/config.yaml
  - Add run-history filtering by status, duration, workflow, and host
acceptance_criteria:
  - Shell completion works for all commands and flags
  - Notifications fire on configurable thresholds (duration, failure)
  - DAG output is deterministic and renders correctly in standard terminals
  - Auto-commit is opt-in only and never enabled by default
notes:
  determinism_rules:
    - DAG visualization is deterministic for the same task graph
    - Completion scripts are generated from the command definition, not hand-maintained
