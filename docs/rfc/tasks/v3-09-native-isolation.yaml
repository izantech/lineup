apiVersion: lineup/v3
kind: RfcTask
id: V3-09
title: Implement native isolation layer
status: todo
last_updated: 2026-04-12
depends_on:
  - V3-00
parallel_wave: 1
suggested_agent: developer
rfc_sections:
  - Final Decisions
  - Rollout Strategy
read_scope:
  - cli/src/lib/process.ts
  - deps/task-foundry/src/isolation.rs
write_scope:
  - cli/src/lib/isolation.ts
  - cli/src/lib/worktree.ts
  - cli/src/lib/process.ts
deliverables:
  - Add index-tier and full-worktree isolation using raw git subprocesses
  - Add sparse isolation behind an explicit feature gate
  - Add cleanup and stale-worktree handling for native runs
acceptance_criteria:
  - Index and full tiers are testable without Task Foundry
  - Sparse mode is disabled by default
  - Cleanup is run-scoped and does not remove unrelated state
notes:
  determinism_rules:
    - Use raw git commands only
    - Keep sparse mode non-default until stability is proven
