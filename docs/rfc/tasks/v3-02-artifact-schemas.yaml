apiVersion: lineup/v3
kind: RfcTask
id: V3-02
title: Add v3 artifact, protocol, and state schemas
status: todo
last_updated: 2026-04-12
depends_on:
  - V3-00
parallel_wave: 1
suggested_agent: developer
rfc_sections:
  - Artifact and Contract Rules
  - Acceptance Criteria
read_scope:
  - cli/src/lib/validation.ts
  - cli/schemas/
  - templates/
write_scope:
  - cli/schemas/
  - cli/src/lib/validation.ts
deliverables:
  - Add JSON Schemas for Constitution, Spec, Plan, Tasks, Review, Config, Protocol, and PipelineState
  - Add validation entrypoints for each new artifact kind
  - Add fixtures for valid and invalid schema coverage
acceptance_criteria:
  - Invalid fixtures fail with stable error messages
  - New artifact schemas can be loaded by AJV
  - Schema validation does not add a second persisted schema stack
notes:
  determinism_rules:
    - Reuse AJV and JSON Schema
    - Keep artifact kinds explicitly versioned with lineup/v3
